#include "main.h"
/**
 * mul - multiplies two integers by one another
 * @a: that's the first argument
 * @b: second argument
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
